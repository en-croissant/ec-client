import { default as GameChessboard } from ".";
import { screen, render } from "@testing-library/react";

describe("GameChessboard", () => {
  xit("successfully makes socket connections", () => {
  });
});
